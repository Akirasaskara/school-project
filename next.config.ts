import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["images.unsplash.com", "picsum.photos", "placeimg.com", "storage.googleapis.com", "cdn.worldvectorlogo.com", "git-scm.com", "encrypted-tbn0.gstatic.com", "upload.wikimedia.org"],
  },
};


export default nextConfig;
